# Tasks

- [x] Initialize Vite React TypeScript project in `./plantiva`
- [/] Install dependencies (`react-router-dom`, `lucide-react`, `tailwindcss`, `@tailwindcss/vite`)
- [ ] Configure Vite for Tailwind CSS v4
- [ ] Create `/pages` and `/components` folders
- [ ] Implement `Navbar` and base Pages (Home, About)
- [ ] Update `App.tsx` and routing configuration
- [ ] Verify build compiles (`npm run build`)
- [ ] Run dev server and confirm connection on localhost
